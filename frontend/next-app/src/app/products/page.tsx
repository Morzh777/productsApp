import { redirect } from "next/navigation";

/**
 * Страница списка товаров
 * 
 * Особенности:
 * - Выполняет редирект на главную страницу для упрощения навигации
 * - Устраняет дублирование контента между /products и /
 * - Обеспечивает единую точку входа для просмотра товаров
 * - Улучшает SEO за счет консолидации URL-адресов
 */
export default function ProductsPage() {
  // Редиректим пользователя на главную страницу, где отображаются товары
  redirect('/');
}